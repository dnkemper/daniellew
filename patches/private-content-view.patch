diff --git a/private_content.module b/private_content.module
index fbe1c16..cb6ac57 100644
--- a/private_content.module
+++ b/private_content.module
@@ -127,7 +127,7 @@ function private_content_node_access(NodeInterface $node, $op, AccountInterface
   // Apply restrictions on private nodes, except for the owner.
   $owner = (!$account->isAnonymous()) && ($node->getOwnerId() == $account->id());
   if (!$owner && $node->private->isPrivate()) {
-    if (($op == 'update') || ($op == 'delete')) {
+    if (($op == 'update') || ($op == 'delete') || ($op == 'view')) {
       if (!$account->hasPermission('edit private content')) {
         // Missing access for write.
         return AccessResult::forbidden()->cachePerPermissions()->cachePerUser()->addCacheableDependency($node);
@@ -158,7 +158,17 @@ function private_content_entity_field_access($operation, FieldDefinitionInterfac
  */
 function private_content_form_node_form_alter(&$form, FormStateInterface &$form_state) {
   if (isset($form['private'])) {
-    $form['private']['#group'] = 'options';
+    $form['private_settings'] = [
+      '#type' => 'details',
+      '#title' => 'Private Content',
+      '#collapsed' => TRUE,
+      '#collapsible' => TRUE,
+      '#tree' => FALSE,
+      '#weight' => -3,
+      '#group' => 'advanced',
+      '#attributes' => ['class' => ['rabbit-hole-settings-form']],
+    ];
+    $form['private']['#group'] = 'private_settings';
   }
 }
 
